# QuaGenius 拡張機能追加 & 税理士資格対応 計画書

ユーザー様の勉強効率を最大化するため、ご提案した拡張機能（音声読み上げ、PDFエクスポート、学習時間の視覚的トラッキング）の実装と、新規資格「税理士」の追加に向けた具体的な実装計画です。

---

## User Review Required

> [!IMPORTANT]
> ### 1. 外部ライブラリを一切使わない「超高速・高調和グラフ」の採用
> 統計モーダルの「出題履歴のグラフ化（学習時間のトラッキング）」において、Chart.jsなどの重い外部ライブラリをロードするのではなく、**Vanilla CSS Grid / Flexbox を活用したモダンで軽量なバーチャート（棒グラフ）**を自作します。これにより、ページの表示速度を一切落とさず、アプリ本来の美しいガラスモーフィズムと完全に調和したUIを実現します。
> 
> ### 2. PDFエクスポートの手法
> 外部のPDF生成ライブラリ（html2pdf等）は日本語フォントの崩れやロードの遅さが課題となるため、**ブラウザ標準の印刷機能（`window.print()`）と印刷専用CSS（`@media print`）を組み合わせた手法**を採用します。
> 印刷ボタンを押すと、不要なボタンや設定フォーム、ヘッダーが自動的に非表示になり、現在解いている「問題文」と「詳細な解説文」だけが美しくフォーマットされた状態でPDF保存（または印刷）できるようになります。

---

## Proposed Changes

今回の変更対象は、プロジェクトのコアファイルである `app.js`、`index.html`、`style.css` の3つすべてです。

### 1. HTML構造の拡張
#### [MODIFY] [index.html](file:///c:/Users/Owner/ai-quiz-app/ai-quiz-app/index.html)
- 資格選択（`#examSelect`）に「税理士（`tax`）」のオプションを追加。
- 解答結果エリア（`#resultSection`）に、以下の制御ボタンを追加：
  - **🔊 解説読み上げボタン** (`#ttsBtn`)
  - **📄 PDF印刷・保存ボタン** (`#printBtn`)
- 統計モーダル（`#statsModal`）に、以下の要素を追加：
  - **⏱ 本日の学習時間表示** (`#todayStudyTime`)
  - **📊 直近7日間の学習進捗グラフコンテナ** (`#weeklyChartContainer`)

### 2. スタイリングの追加と印刷用CSS
#### [MODIFY] [style.css](file:///c:/Users/Owner/ai-quiz-app/ai-quiz-app/style.css)
- 読み上げボタン・PDFボタンのスタイル定義（ガラスモーフィズムに調和するスタイリッシュなアイコンボタン）。
- 統計モーダル内の**CSS製週次進捗グラフ**のスタイル（アニメーション付きのバー、グリッド線、ホバー時の数値ポップアップなど）。
- **印刷専用スタイル (`@media print`)** の追加：
  - 印刷時、背景のグラデーションやモーダル、ボタン、ヘッダー、コントロールパネルなどをすべて非表示（`display: none`）に設定。
  - 問題文と解説文のみを白背景・黒文字（インク節約仕様）で美しく整形して印刷領域に展開。

### 3. ロジックの実装
#### [MODIFY] [app.js](file:///c:/Users/Owner/ai-quiz-app/ai-quiz-app/app.js)
- **「税理士」データ定義の追加**:
  - `EXAM_DATA.tax` を定義し、主要科目（簿記論、財務諸表論、所得税法、法人税法、相続税法、消費税法）をリスト化。
- **学習時間トラッキング機能**:
  - ページを開いている間、`setInterval` を用いて秒単位で学習時間をカウントし、`localStorage` の `quagenius_stats.dailyHistory` に `{ "YYYY-MM-DD": { count: X, time: Y } }` の形式で毎日自動的に累積記録。
  - 統計モーダルを開いた際、この履歴から直近7日間のデータを集計し、CSSのグラフ要素を動的に構築・描画。
- **音声読み上げ（Text-to-Speech）機能**:
  - Web Speech APIの `SpeechSynthesisUtterance` を使用。
  - Markdown解説文からプレーンテキストを抽出（または適度に整形）し、自然な日本語音声で読み上げ。
  - 再生/一時停止/停止のトグル制御を実装。問題の切り替えやスキップ時には自動で停止させるセーフティロジックを実装。
- **PDF印刷連携**:
  - `window.print()` をキックするシンプルなハンドラーの実装。

---

## Verification Plan

### 自動・手動検証
1. **新規資格の確認**:
   - 資格選択で「税理士」を選択し、科目が「簿記論」等に切り替わるか確認。
   - 税理士向けの問題が正常に生成され、解説が表示されるか確認。
2. **音声読み上げ機能の検証**:
   - 解説表示後、「🔊 読み上げる」ボタンを押して日本語音声が流れるか確認。
   - 読み上げ中に「停止」を押す、または「次の問題へ」を押すと音声が止まるか確認。
3. **PDF出力の検証**:
   - 「📄 PDF保存」ボタンを押し、印刷プレビューが開くか確認。
   - プレビュー内で、不要なボタンやコントロールパネルが消え、問題と解説だけが綺麗にレイアウトされているか確認。
4. **学習履歴トラッキングの検証**:
   - ページを開いた状態で時間が経過したとき、学習時間が加算されているか確認。
   - 統計モーダルを開き、学習時間および直近7日間の棒グラフが綺麗に描画されているか確認。
