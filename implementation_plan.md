# QuaGenius ダッシュボードUI改善 & TTS速度調整機能 実装計画書

ユーザー様からのフィードバックに基づき、ダッシュボード（統計モーダル）の縦スクロールを削減して一画面で学習状況を見渡せるようにするための「2カラムレイアウト設計」と、音声読み上げ（TTS）の速度調整スライダー（0.5倍〜2.0倍）の追加に向けた具体的な実装計画です。

---

## User Review Required

> [!IMPORTANT]
> ### 1. 統計モーダル（ダッシュボード）の2カラム・レスポンシブ設計
> 現在の統計モーダルは縦スクロールが多くなってしまっているため、**PCなどの大画面では左右2カラムのグリッドレイアウト**に再設計します。
> *   **左カラム**: 「成績サマリー（総出題数、正答率、本日の学習時間）」および「直近7日間の学習状況（棒グラフ）」
>   - これにより、実績値とグラフが視覚的に横並びになり、スッキリ収まります。
> *   **右カラム**: 「分野別の成績（スクロール可能なリスト）」
>   - 縦長のリストが右側に配置されるため、モーダル全体の高さを抑えつつ一画面に収まります。
> *   **スマートフォン対応 (レスポンシブ)**: 
>   - 画面幅 768px 以下では自動的に「縦1カラム」に綺麗に折りたたまれ、フォントサイズやグラフの横幅もスマホ画面に完全にフィットさせます。
> 
> ### 2. 音声読み上げ（TTS）速度調整スライダーの実装
> 解説エリアの上部のコントロールボタン群（音声読み上げ / PDF保存）の間に、**「⚡ 速度: 0.5x 〜 2.0x」を調整できるスタイリッシュなスライダー（シークバー風コントロール）**を自然に組み込みます。
> *   スライダーを左右にドラッグすることで、いつでも直感的に読み上げ速度を「0.1倍刻み」で調整可能にします。
> *   読み上げ中に速度スライダーを動かした場合、即座に新しい速度で読み上げを再起動する「リアルタイム反映ロジック」を実装し、超快適な操作感を実現します。

---

## Proposed Changes

### 1. HTML構造の拡張
#### [MODIFY] [index.html](file:///c:/Users/Owner/ai-quiz-app/ai-quiz-app/index.html)
- `result-actions` コンテナの中に、速度調整用のスライダー要素（`#ttsSpeed`）と速度表示用のテキスト（`#ttsSpeedVal`）を追加。
- 統計モーダル（`#statsModal`）内の `modal-content` に、2カラム化のためのコンテナラッパー（`#statsDashboardGrid`）を新設。

### 2. スタイリングの追加とレイアウト再構築
#### [MODIFY] [style.css](file:///c:/Users/Owner/ai-quiz-app/ai-quiz-app/style.css)
- **統計モーダルの拡張**:
  - `max-width: 600px;` から `max-width: 850px;` に広げ、大画面での視認性を極限まで高める。
  - `#statsDashboardGrid` に対する `display: grid; grid-template-columns: 1.2fr 1fr; gap: 1.5rem;` の適用。
- **スライダー（シークバー）の装飾**:
  - ガラスモーフィズムと調和する半透明のインプットスタイルと、ホバー時のインタラクション。
  - モバイル時には `flex-wrap: wrap` によりボタンとスライダーが美しく2段構成に折りたたまれるレスポンシブ設計。
- **スマホ用メディアクエリの強化**:
  - `@media (max-width: 768px)` 内で、`#statsDashboardGrid` を 1カラム（`grid-template-columns: 1fr;`）に変更。
  - モーダル内の余白や文字サイズを調整し、スマホでも突き抜けず綺麗に表示。

### 3. ロジックの実装
#### [MODIFY] [app.js](file:///c:/Users/Owner/ai-quiz-app/ai-quiz-app/app.js)
- **DOM要素の取得**:
  - `ttsSpeed`（スライダー）と `ttsSpeedVal`（テキスト）の参照を追加。
- **速度変化の監視**:
  - `ttsSpeed` の `input` イベントをトリガーに、表示テキストを動的変更（例: `1.2x`）。
  - もしすでに読み上げ実行中（`isSpeaking === true`）だった場合、即座に `startSpeaking()` を呼び出して新しい速度で解説文を途中再生するリアルタイム反映ロジックの実装。
- **TTS再生ロジックの修正**:
  - `startSpeaking()` の中で、スライダーの現在値を `SpeechSynthesisUtterance.rate` に動的代入。

---

## Verification Plan

### 自動・手動検証
1. **ダッシュボードの2カラム表示テスト**:
   - PC画面で統計モーダルを開き、左にグラフ・右に科目リストが綺麗に2カラムで配置されていることを確認。
   - ブラウザ幅を狭めていき（スマホ幅）、自動で縦1カラムにスクロール不要なバランスで収まるか確認。
2. **音声読み上げ速度のテスト**:
   - 速度スライダーを `1.5x` や `1.8x` に変更したとき、解説の読み上げ速度が実際に高速化するか確認。
   - `0.8x` や `0.6x` でゆっくりになるか確認。
   - 読み上げの途中でスライダーを動かした際、即座に新しい速度で再生が切り替わるか確認。
